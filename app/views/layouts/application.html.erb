<!DOCTYPE html>
<html>
  <head>
    <title>CasaDasRunas</title>
    <meta name="csrf-token" content="<%= csrf_token %>">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
